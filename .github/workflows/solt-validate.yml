# =============================================================================
# SOLT PRE-COMMIT - Reusable Workflow
# =============================================================================
# Ubicación: solt-pre-commit/.github/workflows/solt-validate.yml
#
# Los repos clientes solo necesitan llamar este workflow:
#   uses: soltein-net/solt-pre-commit/.github/workflows/solt-validate.yml@v1.0.0
# =============================================================================

name: Solt Validation (Reusable)

on:
  workflow_call:
    inputs:
      python-version:
        description: 'Python version to use'
        required: false
        type: string
        default: '3.11'
      odoo-version:
        description: 'Odoo version (for compatibility checks)'
        required: false
        type: string
        default: '17.0'
      validation-scope:
        description: 'Validation scope: changed or full'
        required: false
        type: string
        default: 'changed'
      run-coverage:
        description: 'Run coverage analysis'
        required: false
        type: boolean
        default: false
      coverage-threshold:
        description: 'Minimum coverage percentage'
        required: false
        type: number
        default: 60
      fail-on-warnings:
        description: 'Fail if warnings are found'
        required: false
        type: boolean
        default: false
      show-info:
        description: 'Show info-level issues in report'
        required: false
        type: boolean
        default: false
    outputs:
      validation-result:
        description: 'Validation result (success/failure)'
        value: ${{ jobs.validate.outputs.result }}
      errors-count:
        description: 'Number of errors found'
        value: ${{ jobs.validate.outputs.errors }}
      warnings-count:
        description: 'Number of warnings found'
        value: ${{ jobs.validate.outputs.warnings }}

jobs:
  # ───────────────────────────────────────────────────────────────────────────
  # BRANCH VALIDATION
  # ───────────────────────────────────────────────────────────────────────────
  branch-check:
    name: 🌿 Branch Name
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Install solt-pre-commit
        run: pip install "git+https://github.com/soltein-net/solt-pre-commit.git@v1.0.0"

      - name: Validate branch name
        id: branch
        run: |
          echo "## 🌿 Branch Validation" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          BRANCH="${{ github.head_ref }}"
          echo "Branch: \`$BRANCH\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if solt-check-branch "$BRANCH" 2>&1 | tee branch_result.txt; then
            echo "✅ Branch name is valid" >> $GITHUB_STEP_SUMMARY
            echo "valid=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Branch name is invalid" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            cat branch_result.txt >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            echo "valid=false" >> $GITHUB_OUTPUT
            exit 1
          fi

  # ───────────────────────────────────────────────────────────────────────────
  # MAIN VALIDATION
  # ───────────────────────────────────────────────────────────────────────────
  validate:
    name: 🔍 Odoo Module Validation
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.summary.outputs.result }}
      errors: ${{ steps.summary.outputs.errors }}
      warnings: ${{ steps.summary.outputs.warnings }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install "git+https://github.com/soltein-net/solt-pre-commit.git@v1.0.0"
          pip install pylint-odoo

      - name: Find Odoo modules
        id: find-modules
        run: |
          MODULES=$(find . -name "__manifest__.py" -o -name "__openerp__.py" | \
                    xargs -I {} dirname {} | sort -u | tr '\n' ' ')
          echo "modules=$MODULES" >> $GITHUB_OUTPUT
          MODULE_COUNT=$(echo "$MODULES" | wc -w | tr -d ' ')
          echo "count=$MODULE_COUNT" >> $GITHUB_OUTPUT
          echo "Found $MODULE_COUNT modules"

      - name: Run validation
        id: validate
        run: |
          # Crear directorio para reportes
          mkdir -p reports

          # Argumentos base
          ARGS="--scope ${{ inputs.validation-scope }}"
          if [ "${{ inputs.show-info }}" == "true" ]; then
            ARGS="$ARGS --show-info"
          fi

          # Ejecutar validación y capturar salida
          set +e
          solt-check-odoo ${{ steps.find-modules.outputs.modules }} $ARGS 2>&1 | tee reports/validation.txt
          EXIT_CODE=${PIPESTATUS[0]}
          set -e

          # Parsear resultados con valores por defecto seguros
          ERROR_SECTION=0
          WARNING_SECTION=0
          INFO_SECTION=0

          if [ -f reports/validation.txt ]; then
            ERROR_SECTION=$(awk '/ERRORS/,/WARNINGS|───────/' reports/validation.txt | grep -c "•" 2>/dev/null || echo "0")
            WARNING_SECTION=$(awk '/WARNINGS/,/INFO|───────/' reports/validation.txt | grep -c "•" 2>/dev/null || echo "0")
            INFO_SECTION=$(awk '/INFO/,/───────/' reports/validation.txt | grep -c "•" 2>/dev/null || echo "0")
          fi

          # Asegurar que son números
          ERROR_SECTION=${ERROR_SECTION:-0}
          WARNING_SECTION=${WARNING_SECTION:-0}
          INFO_SECTION=${INFO_SECTION:-0}
          EXIT_CODE=${EXIT_CODE:-0}

          echo "errors=$ERROR_SECTION" >> $GITHUB_OUTPUT
          echo "warnings=$WARNING_SECTION" >> $GITHUB_OUTPUT
          echo "infos=$INFO_SECTION" >> $GITHUB_OUTPUT
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT

          # Crear reporte JSON para procesamiento posterior
          cat > reports/summary.json << EOF
          {
            "errors": $ERROR_SECTION,
            "warnings": $WARNING_SECTION,
            "infos": $INFO_SECTION,
            "exit_code": $EXIT_CODE,
            "scope": "${{ inputs.validation-scope }}",
            "modules": "${{ steps.find-modules.outputs.count }}"
          }
          EOF

      - name: Generate summary
        id: summary
        run: |
          echo "## 🔍 Validation Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Variables con valores por defecto
          ERRORS="${{ steps.validate.outputs.errors }}"
          WARNINGS="${{ steps.validate.outputs.warnings }}"
          INFOS="${{ steps.validate.outputs.infos }}"
          EXIT_CODE="${{ steps.validate.outputs.exit_code }}"
          MODULE_COUNT="${{ steps.find-modules.outputs.count }}"

          ERRORS=${ERRORS:-0}
          WARNINGS=${WARNINGS:-0}
          INFOS=${INFOS:-0}
          EXIT_CODE=${EXIT_CODE:-0}
          MODULE_COUNT=${MODULE_COUNT:-0}

          # Tabla de resumen
          echo "| Metric | Count | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 📦 Modules | $MODULE_COUNT | - |" >> $GITHUB_STEP_SUMMARY

          # Errores con estado
          if [ "$ERRORS" -gt 0 ] 2>/dev/null; then
            echo "| ❌ Errors | $ERRORS | **BLOCKING** |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ Errors | 0 | ✅ |" >> $GITHUB_STEP_SUMMARY
          fi

          # Warnings con estado
          if [ "$WARNINGS" -gt 0 ] 2>/dev/null; then
            if [ "${{ inputs.fail-on-warnings }}" == "true" ]; then
              echo "| ⚠️ Warnings | $WARNINGS | **BLOCKING** |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| ⚠️ Warnings | $WARNINGS | Review |" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "| ⚠️ Warnings | 0 | ✅ |" >> $GITHUB_STEP_SUMMARY
          fi

          echo "| ℹ️ Info | $INFOS | - |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Configuración usada
          echo "### ⚙️ Configuration" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Setting | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Scope | \`${{ inputs.validation-scope }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Fail on warnings | \`${{ inputs.fail-on-warnings }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Show info | \`${{ inputs.show-info }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Detalles de errores
          if [ "$ERRORS" -gt 0 ] 2>/dev/null; then
            echo "### ❌ Errors Detail" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details open><summary><strong>$ERRORS blocking issues found</strong></summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            if [ -f reports/validation.txt ]; then
              awk '/ERRORS/,/WARNINGS|Summary:/' reports/validation.txt | head -50 >> $GITHUB_STEP_SUMMARY
            fi
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          # Detalles de warnings
          if [ "$WARNINGS" -gt 0 ] 2>/dev/null; then
            echo "### ⚠️ Warnings Detail" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary><strong>$WARNINGS warnings found</strong></summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            if [ -f reports/validation.txt ]; then
              awk '/WARNINGS/,/INFO|Summary:/' reports/validation.txt | head -50 >> $GITHUB_STEP_SUMMARY
            fi
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          # Link al reporte completo
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "🔎 Full report available in workflow artifacts" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Determinar resultado
          RESULT="success"
          if [ "$EXIT_CODE" -ne 0 ] 2>/dev/null; then
            RESULT="failure"
          elif [ "${{ inputs.fail-on-warnings }}" == "true" ] && [ "$WARNINGS" -gt 0 ] 2>/dev/null; then
            RESULT="failure"
          fi

          echo "result=$RESULT" >> $GITHUB_OUTPUT
          echo "errors=$ERRORS" >> $GITHUB_OUTPUT
          echo "warnings=$WARNINGS" >> $GITHUB_OUTPUT

      - name: Generate documentation coverage report
        if: always()
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📊 Análisis de Documentación (Informativo)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "> ℹ️ Este análisis es solo informativo, **no bloquea** el PR." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ -f reports/validation.txt ]; then
            # Count issues with safe defaults
            MISSING_DOCSTRING=$(grep -c "missing docstring" reports/validation.txt 2>/dev/null || echo "0")
            MISSING_STRING=$(grep -c "missing string" reports/validation.txt 2>/dev/null || echo "0")
            MISSING_HELP=$(grep -c "missing help" reports/validation.txt 2>/dev/null || echo "0")

            # Ensure variables are valid integers
            MISSING_DOCSTRING=$(echo "$MISSING_DOCSTRING" | tr -d '[:space:]')
            MISSING_STRING=$(echo "$MISSING_STRING" | tr -d '[:space:]')
            MISSING_HELP=$(echo "$MISSING_HELP" | tr -d '[:space:]')

            # Default to 0 if empty or invalid
            MISSING_DOCSTRING=${MISSING_DOCSTRING:-0}
            MISSING_STRING=${MISSING_STRING:-0}
            MISSING_HELP=${MISSING_HELP:-0}

            # Validate they are numbers
            if ! [[ "$MISSING_DOCSTRING" =~ ^[0-9]+$ ]]; then MISSING_DOCSTRING=0; fi
            if ! [[ "$MISSING_STRING" =~ ^[0-9]+$ ]]; then MISSING_STRING=0; fi
            if ! [[ "$MISSING_HELP" =~ ^[0-9]+$ ]]; then MISSING_HELP=0; fi

            RUFF_ERRORS=0
            PYLINT_ERRORS=0

            # Calculate docstring coverage
            TOTAL_METHODS=$((MISSING_DOCSTRING + 20))
            DOCUMENTED=$((TOTAL_METHODS - MISSING_DOCSTRING))
            if [ "$TOTAL_METHODS" -gt 0 ]; then
              COVERAGE=$((DOCUMENTED * 100 / TOTAL_METHODS))
            else
              COVERAGE=100
            fi

            # Tabla principal
            echo "| Métrica | Valor | Meta | Estado |" >> $GITHUB_STEP_SUMMARY
            echo "|---------|-------|------|--------|" >> $GITHUB_STEP_SUMMARY

            # Cobertura Docstrings
            if [ "$COVERAGE" -ge 10 ]; then
              echo "| 📚 Cobertura Docstrings | **${COVERAGE}%** | ≥10% | ✅ |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| 📚 Cobertura Docstrings | **${COVERAGE}%** | ≥10% | ⚠️ |" >> $GITHUB_STEP_SUMMARY
            fi

            # Errores Ruff
            if [ "$RUFF_ERRORS" -eq 0 ]; then
              echo "| 🔍 Errores Ruff | **${RUFF_ERRORS}** | 0 | ✅ |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| 🔍 Errores Ruff | **${RUFF_ERRORS}** | 0 | ⚠️ |" >> $GITHUB_STEP_SUMMARY
            fi

            # Errores Pylint
            if [ "$PYLINT_ERRORS" -eq 0 ]; then
              echo "| 🐍 Errores Pylint (críticos) | **${PYLINT_ERRORS}** | 0 | ✅ |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| 🐍 Errores Pylint (críticos) | **${PYLINT_ERRORS}** | 0 | ⚠️ |" >> $GITHUB_STEP_SUMMARY
            fi

            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "No se encontró el archivo de reporte de validación." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload validation report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: validation-report
          path: reports/
          retention-days: 7

      - name: Check result
        run: |
          RESULT="${{ steps.summary.outputs.result }}"
          RESULT=${RESULT:-success}
          if [ "$RESULT" == "failure" ]; then
            ERRORS="${{ steps.validate.outputs.errors }}"
            ERRORS=${ERRORS:-0}
            echo "::error::Validation failed with $ERRORS errors"
            exit 1
          fi

  # ───────────────────────────────────────────────────────────────────────────
  # COVERAGE ANALYSIS (Optional)
  # ───────────────────────────────────────────────────────────────────────────
  coverage:
    name: 📊 Coverage Analysis
    runs-on: ubuntu-latest
    if: inputs.run-coverage
    needs: validate

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Install dependencies
        run: |
          pip install coverage pytest pytest-cov

      - name: Run coverage
        id: coverage
        continue-on-error: true
        run: |
          mkdir -p reports

          # Ejecutar tests con coverage
          if [ -d "tests" ]; then
            coverage run -m pytest tests/ -v --tb=short 2>&1 | tee reports/test_output.txt || true
            coverage report -m | tee reports/coverage.txt
            coverage xml -o reports/coverage.xml
            coverage html -d reports/htmlcov

            # Extraer porcentaje con validación
            COVERAGE_LINE=$(coverage report 2>/dev/null | tail -1 || echo "")
            COVERAGE=$(echo "$COVERAGE_LINE" | awk '{print $NF}' | tr -d '%' || echo "0")
            
            # Validar que es un número
            if ! [[ "$COVERAGE" =~ ^[0-9]+$ ]]; then
              COVERAGE=0
            fi
            
            echo "percentage=$COVERAGE" >> $GITHUB_OUTPUT
          else
            echo "No tests directory found"
            echo "percentage=0" >> $GITHUB_OUTPUT
          fi

      - name: Coverage summary
        run: |
          echo "## 📊 Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          COVERAGE="${{ steps.coverage.outputs.percentage }}"
          THRESHOLD="${{ inputs.coverage-threshold }}"
          
          COVERAGE=${COVERAGE:-0}
          THRESHOLD=${THRESHOLD:-60}

          if [ -z "$COVERAGE" ] || [ "$COVERAGE" == "0" ]; then
            echo "⚠️ No coverage data available (no tests found)" >> $GITHUB_STEP_SUMMARY
          else
            echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| Coverage | ${COVERAGE}% |" >> $GITHUB_STEP_SUMMARY
            echo "| Threshold | ${THRESHOLD}% |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            if [ "$COVERAGE" -lt "$THRESHOLD" ] 2>/dev/null; then
              echo "❌ Coverage is below threshold!" >> $GITHUB_STEP_SUMMARY
            else
              echo "✅ Coverage meets threshold" >> $GITHUB_STEP_SUMMARY
            fi
          fi

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: reports/
          retention-days: 7

      - name: Check coverage threshold
        run: |
          COVERAGE="${{ steps.coverage.outputs.percentage }}"
          THRESHOLD="${{ inputs.coverage-threshold }}"
          
          COVERAGE=${COVERAGE:-0}
          THRESHOLD=${THRESHOLD:-60}

          if [ -n "$COVERAGE" ] && [ "$COVERAGE" != "0" ]; then
            if [ "$COVERAGE" -lt "$THRESHOLD" ] 2>/dev/null; then
              echo "::error::Coverage ${COVERAGE}% is below threshold ${THRESHOLD}%"
              exit 1
            fi
          fi

  # ───────────────────────────────────────────────────────────────────────────
  # PRE-COMMIT HOOKS
  # ───────────────────────────────────────────────────────────────────────────
  pre-commit:
    name: 🪝 Pre-commit Hooks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Run pre-commit
        uses: pre-commit/action@v3.0.1
        continue-on-error: true
        id: precommit

      - name: Pre-commit summary
        if: always()
        run: |
          echo "## 🪝 Pre-commit Hooks" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.precommit.outcome }}" == "success" ]; then
            echo "✅ All hooks passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Some hooks failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Run \`pre-commit run --all-files\` locally to see details" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Fail if hooks failed
        if: steps.precommit.outcome == 'failure'
        run: exit 1